# Thilanga portfolio

Home section only. Run `node server.cjs` and open http://127.0.0.1:4173.

Static website files live in `dist/`. Edit identity and copy in `dist/index.html`, shared styling in `dist/styles.css`, and navigation/dialog/reveal behavior in `dist/script.js`.

The portrait is original generated cyber artwork, not a photo of the portfolio owner. Replace `dist/assets/cyber-portrait.png` when a personal photo is supplied. The name is an editable assumption. Real email and social profile URLs have not been supplied; the connect dialog explicitly marks these as coming soon.

Add subsequent sections only after receiving their reference images. Preserve Home. Add actual navigation anchors when those sections exist; the observer automatically highlights them.
